import numpy as np

def neighbourhood_exploration(X,n,max_r):
    r = #function to update r
    new_sols = np.zeros(len(X))
    for i in range(0,n):
        sol = # function to generate a sol
        evaluation = #function to evaluate sol
        new_sols[i] = sol
    # sort
    # discard worst
    # distribute Cx
    return new_sols
